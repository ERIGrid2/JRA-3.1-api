---
get:
  summary: Get transport status
  operationId: getStatus

  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Status.yaml
