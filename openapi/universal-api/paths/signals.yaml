---
get:
  tags:
  - signals
  summary: List all available signals
  operationId: getSignals
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/SignalIdList.yaml
