---
get:
  tags:
  - transport
  summary: Start transport
  operationId: startTransport

  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Info.yaml
