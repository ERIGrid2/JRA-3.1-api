---
get:
  tags:
  - channels
  summary: List all available channels
  operationId: getChannels
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ChannelDescriptionList.yaml
